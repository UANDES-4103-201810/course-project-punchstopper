<%= form_with(model: user_wishlist, local: true) do |form| %>
  <div class="project_item">
  <% if user_wishlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_wishlist.errors.count, "error") %> prohibited this user_wishlist from being saved:</h2>

      <ul>
      <% user_wishlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_wishlist_user_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :user_wishlist_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>
