<div class="project_item">
  <h1>Back this Project</h1>

  <%= form_tag(:controller => "projects", :action => "create_funding") do %>
    <p>
      <%= label_tag :amount %>
      <%= number_field_tag(:amount) %>
    </p>
    <p>
      <%= submit_tag "Submit"%>
    </p>
  <% end %>

  <%= link_to 'Back', projects_path %>
</div>