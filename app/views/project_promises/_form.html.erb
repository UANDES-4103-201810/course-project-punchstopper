<div class="project_item">
<%= form_with(model: project_promise, local: true) do |form| %>
  <% if project_promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_promise.errors.count, "error") %> prohibited this project_promise from being saved:</h2>

      <ul>
      <% project_promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_promise_project_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :project_promise_description %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost, id: :project_promise_cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>